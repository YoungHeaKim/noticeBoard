<!DOCTYPE html>
<html lang="en">
<head>
  <title>Test-Board</title>
</head>
<body>
  <header>
    <h1>Test-Board</h1>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <li>
          <a href="/user/edit">User 수정</a>
        </li>
        <li>
          <a href="/user/logout">Logout</a>
        </li>
      </ul>
    </nav>
  </header>
  <main>
    <div class="buttons">
      <a href="/article/lists">Back</a>
      <% if(articleList.writer === user._id) { %>
        <a href="/lists/<%= articles.id %>">수정</a>
        <form action="/lists/<%= articles.id %>" method="post">
          <a href="#" onclick="confirm('Do you want to delete this?')?this.parentElement.submit():null;">삭제</a>
        </form>
      <% } %>
    </div>
    <section>
      <h2><%= articleList.title %></h2>
      <div>
        <div>
          <span>Writer</span> : <%= username %>
        </div>
        <div>
          <span>Created</span> :
          <%= articleList.createdDate %> <%= articleList.createdDate %>
        </div>
        <% if(articleList.updatedAt) { %>
          <div>
            <span>updated</span> :
            <%= articleList.updatedAt %> <%= articleList.updatedDate %>
          </div>
        <% } %>
      </div>
      <div>
        <%= articleList.description %>
      </div>
    </section>
  </main>
</body>
</html>